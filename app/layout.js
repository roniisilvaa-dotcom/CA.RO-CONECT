export const metadata = { title: 'CA.RO Connect', description: 'Conversational Commerce Platform' }
export default function RootLayout({ children }) {
  return <html lang="pt-BR"><body>{children}</body></html>
}
